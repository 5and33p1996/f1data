export const metadata = {
  title: 'F1 Data',
  description: 'Get Formula 1 Data',
}
 
export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
